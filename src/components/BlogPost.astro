---
const { url, title, author, image, description,  pubDate} = Astro.props;
console.log(Astro.props)
---
<a href={url}><div class="post">
    <img src={image} width="350px" />
 <h2>{title}</h2> 
   <p>{description}</p>
    <em>{author}</em> - 
    <span>{pubDate}</span>
</div></a>
<!-- <li><a href={url}>{title}</a> - {author} - <span>{pubDate}</span></li> -->
